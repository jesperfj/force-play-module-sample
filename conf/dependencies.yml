# Application dependencies

require:
    - play
    - force -> force
    
repositories:
    - My modules:
        type:       local
        artifact:   ${application.path}/[module]
        contains:
            - force
